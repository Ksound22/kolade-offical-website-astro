---
import Theme from './Theme.astro';
const pathname = Astro.url.pathname;
import logo from '../../public/kolade-logo.png'
import { Image } from 'astro:assets';
---

<nav>
  <a href="/">
    <Image
      src={logo}
      alt="Kolade Chris"
      height={0}
      width={0}
      sizes='100vw'
      alt=""
    >
  </a>
 
  <ul class="navigation">
    <li class="navigation-item">
      <a href="/subscribe" class={pathname === "/subscribe" ? "active" : ""}>Subscribe</a>
    </li>
    <li class="navigation-item">
      <a href="/about" class={pathname === "/about" ? "active" : ""}>About</a>
    </li>
    <li class="navigation-item">
      <a href="/blog"  class={pathname === "/blog" ? "active" : ""}>Blog</a>
    </li>
    <li class="navigation-item">
      <a href="/services"  class={pathname === "/services" ? "active" : ""}>Services</a>
    </li>
    <li class="navigation-item">
      <a href="/contact"  class={pathname === "/contact" ? "active" : ""}>Contact</a>
    </li>
    
    <Theme />
  </ul>
  <button class="burger-menu" id="burger-menu">
    <ion-icon name="menu-outline" class="bars"></ion-icon>
    <ion-icon name="close-outline" class="x" ></ion-icon>
  </button>
</nav>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  ul {
    list-style-type: none;
  }

  a {
    text-decoration: none;
  }

  nav {
    position: sticky;
    top: 0;
    left: 0;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem 3.5rem;
    background-color: var(--navbar-and-footer-bg);
   border-bottom: 0.5px solid gray;
  }


  nav a {
    color: unset;
    transition: var(--transition);
  }
  
  nav ul {
    display: flex;
    gap: 2.9rem;
  }
  
  nav ul li {
    font-size: 18px;
    font-weight: 700;
  }

  nav ul li a:hover {
    border-bottom: 2px solid var(--primary-color);
  }

  .burger-menu {
    color: var(--text-color);
    font-size: 2rem;
    border: 0;
    background-color: transparent;
    cursor: pointer;
    display: none;
  }

  .burger-menu .x {
    display: none;
  }

  .burger-menu.open .bars {
    display: none;
  }

  .burger-menu.open .x {
    display: block;
  }

  ion-icon {
   color: var(--primary-color);
   font-size: 54px;
  }

  ion-icon:hover {
   color: var(--primary-hover);
   cursor: pointer;
  }

  .active {
    border-bottom: 2px solid var(--primary-color);
  }

  @media screen and (max-width: 720px) {
    nav {
      padding: 1.5rem 1rem;
    }

    nav ul {
      position: fixed;
      background-color: var(--navbar-and-footer-bg);
      flex-direction: column;
      top: 8.9rem;
      right: 0;
      width: 50%;
      transform: translateX(120%);
      transition: transform var(--transition) ease-in;
      gap: 1.2rem;
      padding: 1rem 1.2rem;
    }

    nav ul li {
      margin: 8px;
    }

    .burger-menu {
      display: block;
    }

    nav ul.show {
      transform: translateX(0);
    }

  }

  @media screen and (max-width: 420px) {
  /*  */
  }
</style>

<script>
  const burger = document.querySelector("#burger-menu");
  const ul = document.querySelector("nav ul");
  // const navLink = document.querySelectorAll(".nav-link");

  burger.addEventListener("click", () => {
    ul.classList.toggle("show");
    burger.classList.toggle("open");
  });

  // navLink.forEach((link) => link.addEventListener("click", () => {
  //   ul.classList.remove("show");
  //   burger.classList.remove("open");
  // }));
</script>